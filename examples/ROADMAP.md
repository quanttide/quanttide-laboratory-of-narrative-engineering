# 实验路线图

p01-p10 覆盖了母题和风格两条管线。下一阶段实验聚焦三个盲区：情节建模、跨系列验证、契约-内容分层。

---

## 待执行

| PoC | 假设 | 优先级 | 目录 |
|-----|------|--------|------|
| **p11** 情节一致性检测 | LLM 可基于 story.yaml 检测跨场景的人物行为一致性 | 🔴 高 | `p11-plot-consistency/` |
| **p12** 情节-母题联动 | 情节转折点由母题承载——关键情节时刻对应特定母题的出现 | 🔴 高 | `p12-plot-motif-linkage/` |
| **p13** 跨系列审美转移 | 都市言情的 style.yaml 可部分应用于校园言情，genre_fit=3 维度跨系列稳定 | 🟡 中 | `p13-cross-series-transfer/` |
| **p14** 契约层生成验证 | 以 style+motif+story 三份 YAML 为契约生成场景，验证三层约束的同时满足率 | 🟡 中 | `p14-contract-generation/` |
| **p15** 内容层扩展 | 母题/风格提取管线可应用于非虚构内容（brochure/history），叙事模式跨媒介迁移 | 🟢 低 | `p15-content-expansion/` |

---

## p11 — 情节一致性检测

### 假设

1. story.yaml 的 14 章场景描述构成可验证的情节基线
2. LLM 能检测跨场景的人物行为矛盾（如林远亭在某个场景说"我哭不出来了"但在另一个场景哭了出来——这不是矛盾，这是情节弧线）
3. LLM 能区分"真正的情节矛盾"和"有意的人物弧线"

### 实验设计

**数据**：story.yaml 14 章 + 原文，手动植入 3 处人工矛盾（如篡改某个细节让两个场景的时间线冲突）。

**步骤**：LLM 逐对比较相邻场景 → 标注 inconsistencies → 人工验证哪些是真正的矛盾、哪些是有意弧线。

### 验收

| 指标 | 合格线 |
|------|--------|
| 人工植入的 3 处矛盾全被检测 | 3/3 |
| 误报率（将有意的弧线标记为矛盾） | < 30% |

---

## p12 — 情节-母题联动

### 假设

1. 小说的每个情节转折点都对应一个或多个母题的出现
2. LLM 能识别这种对应关系——如"递毛巾"同时是情节转折（两人第一次身体接触）和手势母题的体现

### 实验设计

**数据**：story.yaml 的 14 章 + motif.yaml 的 5 母题。人工标注每个场景使用了哪些母题（ground truth 来自 p05 的提取结果）。

**步骤**：LLM 判断每个场景的情节功能（起/承/转/合）→ LLM 提取每个场景的母题 → 计算情节功能与母题分布的关联强度。

### 验收

| 指标 | 合格线 |
|------|--------|
| "转"和"合"场景的母题密度 > "承"场景 | 显著差异 |
| "手势"母题出现在关键身体接触场景的频率 > 随机分布 | p < 0.1 |

---

## p13 — 跨系列审美转移

### 假设

1. p09 验证的 genre_fit 分类可重现：genre_fit=3 维度（细节美学、整体美学）跨系列稳定，genre_fit=1 维度（情感表达）系列专属
2. 跨系列评审的评分衰减量与 genre_fit 分类一致

### 实验设计

**数据**：都市言情 style.yaml + 校园言情 style.yaml。测试文章：都市成稿 2 篇 + 校园成稿 2 篇。

**步骤**：都市 style 评校园文章 → 校园 style 评都市文章 → 对比 genre_fit=3 vs genre_fit=1 维度的分差。

### 验收

| 指标 | 合格线 |
|------|--------|
| genre_fit=3 维度跨系列分差 | < 1.5 |
| genre_fit=1 维度跨系列分差 | > 2 |
| 两位作者各自的 style 评审自身文章 > 跨系列评审 | 方向一致 |

---

## p14 — 契约层生成验证

### 假设

1. style+motif+story 三份 YAML 构成完整生成约束
2. 在契约约束下生成的场景比无约束场景在所有三层指标上都更好
3. 缺失任何一层约束都会导致对应指标的下降

### 实验设计

**数据**：都市言情 style+motif+story。场景模板：从 story.yaml 中选取 3 个已有场景（跳过，让 LLM 重新生成同一场景的新版本）。

**步骤**：四组生成（全约束 / 缺 style / 缺 motif / 无约束）→ 分别评审风格一致性 + 母题覆盖 + 情节连贯性。

### 验收

| 指标 | 合格线 |
|------|--------|
| 全约束组在三项指标上均 > 无约束组 | 方向一致 |
| 缺 style 组风格分 < 全约束组 | 下降 > 20% |
| 缺 motif 组母题覆盖 < 全约束组 | 下降 > 20% |

---

## p15 — 内容层扩展

### 假设

1. 母题提取和风格评审管线可应用于非虚构内容
2. 宣传册（brochure）文本中可识别出与小说类似的叙事模式（如"使命叙述"作为母题变体）
3. 事实度越高的内容，母题信号越弱但风格信号越稳定

### 实验设计

**数据**：assets/brochure 的公司简介 + assets/history 的品牌故事片段。对照：都市言情成稿。

**步骤**：对三类文本（小说/宣传册/品牌故事）分别跑 extract + review → 对比母题密度和风格评分分布。

### 验收

| 指标 | 合格线 |
|------|--------|
| 宣传册文本可提取出 ≥ 1 个叙事母题 | 可提取 |
| 风格评审管线在非虚构文本上正常运行 | 不报错 |
| 小说母题密度 > 宣传册母题密度 | 方向一致 |
