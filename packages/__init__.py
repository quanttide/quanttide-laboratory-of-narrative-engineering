# packages — p16 实验可复用组件
#
# 使用方法：
#   sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
#   from packages.llm import call_llm, clean_json
#   from packages.io import read_article_text, save_json, ensure_dir
